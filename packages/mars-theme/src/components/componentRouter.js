import React from "react";
import { connect } from "frontity";
import IntroSection from "./partials/introSection";

const ComponentRouter = ({ state }) => {
  
  return (

  );
};

export default connect(ComponentRouter);
